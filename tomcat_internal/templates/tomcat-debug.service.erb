# MANAGED BY PUPPET

[Unit]
Description=Apache Tomcat (Debug)
After=syslog.target network.target
Conflicts=tomcat.service

[Service]
EnvironmentFile=/www/tomcat/base/current/conf/tomcat-debug.conf

ExecStart=/www/tomcat/base/current/bin/catalina.sh run
ExecStop=/www/tomcat/base/current/bin/catalina.sh stop
SuccessExitStatus=143

User=<%= scope['tomcat::user'] %>
Group=<%= scope['tomcat::group'] %>

SyslogIdentifier=tomcat

[Install]
WantedBy=multi-user.target
